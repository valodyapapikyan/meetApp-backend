export enum providers {
  LINKEDIN = 'linkedin',
  GOOGLE = 'google',
  GITHUB = 'github',
}

export enum HTTP_STATUS {
  SUCCESS = 200,
  CREATED = 201,
  ANAUTHORIZED = 401,
  BAD_REQUEST = 400,
  NOT_FOUND = 404,
}

export enum RESPONSE_STATUS {
  SUCCESS = 'success',
  ERROR = 'error',
}
